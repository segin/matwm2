xmacro
macro local variables
macro local labels (possibly macro)
db directive for store bytes separate and text with ""
dw, dd, dq, etc
support pagesel pseudo instruction
changing default radix
include search path
relocatable code and coff output support
add ihex thing for bigger than 16bit addresses
header files for chips
support more chips
fancy disassembler output that can be re-assembled as-is option
install target for maekfile
if writing to stdout, write to memory first, so on error there is not half-finished garbage printed

BUGS
	if integer size on host architecture is less than 32bit all will fail
	unicode will be case sensitive
	should ppsub arguments of macro (both macro directive and when macros used)
