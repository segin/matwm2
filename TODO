fix/rewrite focus system
go through bugs and figure out why i have 2 changelog files (merge them or just ditch in favor of git log?)
(optional) resize method that allows picking corners other than bottom right
option to change settings for windows that match a given regex
themes support
write a small toolkit that can do menus and dialogs and use it to give a menu for windows etc
fix spawn() on Windows (Windows the OS)
--release 0.2.0
action to center windows (or perhaps more flexible feature that allows this)
